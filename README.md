# snowflake-coco-predictive-maintenance

Trailing-window Engine Head (`CNC_HORIZONTAL`) maintenance/sensor data was hand-adjusted post-generation (SH-42) to ensure the priority-flip narrative; underlying generation logic (`simulate.py`) is unchanged.
